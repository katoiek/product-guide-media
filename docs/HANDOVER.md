# えらびノート 引き継ぎ書

最終更新: 2026-08-09

一般商品比較メディア「えらびノート」の状況を次の担当者へ引き継ぐ文書です。
**認証情報、APIキー、トークン、パスワード、秘密鍵はこのリポジトリや本書に保存しません。**

---

## 0. 新しいセッションで最初にやること

この3つを順に実行すれば、現状を把握できます。

```bash
node tools/queue.mjs status   # 全記事の状態と次の担当エージェント
node tools/queue.mjs next     # 次に着手すべき1件
npm run gate                  # 検証 + ビルド。合格していれば健全
```

そのうえで読む順番:

1. **本書**（媒体の状況、いま何が止まっているか）
2. `docs/AUTOMATION.md`（パイプラインの仕組みと運用手順）
3. `content/pipeline/policy.json`（公開ポリシー。**唯一の正**）
4. `.claude/skills/erabi-pipeline/SKILL.md`（エージェントへの委譲手順と過去の事故）

**リポジトリは公開設定です。** 収益額・認証情報をコミットしないでください。

---

## 1. 媒体の概要

| 項目 | 内容 |
| --- | --- |
| 媒体名 | えらびノート |
| 公開URL | `https://erabi-note.jp/` |
| リポジトリ | `katoiek/product-guide-media`（**PUBLIC**） |
| フレームワーク | Astro（静的サイト） |
| ホスティング | Cloudflare Pages（`main` へのpushで自動デプロイ） |
| 目的 | 同じ購入目的を満たす異なるメーカーの製品を比較し、購入条件から選べるようにする |

読者像は「欲しい商品ジャンルは決まっているが、複数メーカーのどれを選ぶべきか決められない人」です。

---

## 2. いま公開しているもの（7記事）

すべて比較記事です。購入導線を持たない一般ガイドは削除済みで、ゼロです。

| 記事 | 製品 | 購入導線 |
| --- | --- | --- |
| 猫砂（固まるタイプ） | 7社 | あり |
| 食洗機用洗剤 | 6社 | あり |
| 洗濯用液体洗剤 | 6社 | あり |
| キッチンスポンジ | 6社 | あり |
| 洗濯用漂白剤 | 7製品5社 | あり |
| **犬用ペットシーツ** | 5社 | **なし（ASIN未取得）** |
| **猫用トイレ消臭シート** | 6社 | **なし（ASIN未取得）** |

削除した記事（`public/_redirects` で404を返す）:
`/articles/storage-box-sizing/`、`/articles/keyboard-selection-axis/`、`/articles/bousai-supplies-checklist/`、`/contact/`

---

## 3. 次に必要なアクション（優先順）

### ① 新記事2本にASINを入れる（すぐできる・収益に直結）

犬用ペットシーツと猫用トイレ消臭シートは**記事が公開済みなのに購入リンクがありません**。

```bash
npm run asin:sheet    # CSVを再生成（記入済みは引き継がれる）
```

`content/pipeline/asin-input/asins.csv` の該当行にASINを記入 → 取り込み → リンク差し込み。
手順は `docs/ASIN-INPUT.md` にあります。

### ② フローリング用ドライシートを記事化（仕様データは取得済み）

`content/pipeline/specs/floor-dry-sheet-comparison.json` に5製品5社ぶんが揃っています。
`erabi-article-writer` に渡せば書けます。記事の核は「**手持ちのワイパーに合うか**」で、
ユニ・チャームは製品名に「＜各社共通タイプ＞」と明記、花王は自社ライン扱いという分かれ方です。

### ③ アソシエイト審査の期限管理（放置すると失う）

| 項目 | 値 |
| --- | --- |
| 申請日 | 2026-08-08 |
| **期限** | **2027-02-04** |
| 条件 | 適格販売3件 |

**達成しないとアカウントが取り消されます。** 達成すると PA-API が使えるようになり、
ASINの手入力が不要になります（`tools/amazon-fetch.mjs` が実装済み）。

### ④ 収益計測を回す（データが出たら）

```bash
npm run earnings:import -- <report.csv> --month YYYY-MM
npm run revenue
```

アソシエイト・セントラルのレポートを取り込むと、記事別の成果と
「**読者が買ったがリンクしていない商品**」が出ます。後者が次のテーマの最有力候補です。

**リポジトリが公開のため金額はコミットされません**（`content/pipeline/revenue/` は `.gitignore`）。
`queue.json` に入るのは順位と区分だけです。

### ⑤ 週次の自動運用を有効化（任意）

`.github/workflows/pipeline.yml` が火曜=新規1本／金曜=既存の再確認で動きます。
有効化には GitHub Secrets に `ANTHROPIC_API_KEY` と、Workflow permissions を
"Read and write" にする設定が必要です。未設定の間はジョブが自動スキップされます。

---

## 4. キューの状態

`node tools/queue.mjs status` が常に正です。2026-08-09 時点の要約:

| state | 件数 | 内容 |
| --- | --- | --- |
| `published` | 7 | 上記の公開記事 |
| `assets_pending` | 3 | ペットシーツ・消臭シート（ASIN待ち）、ドライシート（記事化待ち） |
| `idea` | 6 | 浄水器カートリッジ、モニターアーム、剪定ばさみ、シュレッダー、衣類収納ケース、USBハブ |
| `blocked` | 2 | キーボード2件（依頼により保留） |

`idea` の6件はトレンド調査で登録済みで、ブランド候補と価格帯が入っています。
**浄水器カートリッジだけが消耗品**で、残り5件は耐久財（`profile: durable`）です。

---

## 5. 作業するときの必須ルール

### エージェントの報告を信用しない

**実運用で5件の誤りを検出しています。** すべて公開前に見つけました。

| 種類 | 例 |
| --- | --- |
| 製品名が実ページと違う | アリエール、食洗機用ジョイ（いずれもP&G） |
| 裏付けのない断定 | エステーの他社対応、アイリスオーヤマの「各社共通」 |
| 基準を満たすための水増し | 種類が確認できない漂白剤を社数合わせで採用 |

**必ず公式ページを1〜2件開いて、製品名・数値を突き合わせてください。**
特にP&Gなど、ブランドサイトの表記と流通名が違うメーカーは要注意です。

### 委譲するときは事実を要約して渡さない

仕様データを要約して執筆エージェントに渡した結果、**取りこぼした事実で記事が
根拠と矛盾しかけた**ことがあります（キッチンスポンジの抗菌表示）。

渡してよいのは**着眼点**だけです。プロンプトには必ず
「**私の指示と仕様データが食い違う場合は、必ず仕様データ側を正とする**」を入れてください。

### 検証が終わる前に執筆を走らせない

猫用トイレ消臭シートで、訂正が届く前に記事が書き上がり、**誤った内訳のまま完成**しました。
書き直しになりました。**仕様データの突き合わせを終えてから執筆を開始してください。**

### 散文で判定させず、機械可読のキーを持たせる

「対応するトイレ本体: 専用か他社対応かの明記は確認できず」という文字列を
「専用」と誤判定した事故がありました。**判断が分かれる項目は `対応区分` のような
独立したキー**にして、値を定型にしてください。消臭シートとドライシートで採用済みです。

### 複数エージェントに共有ファイルを同時編集させない

`src/pages/index.astro` と `public/sitemap.xml` は更新が失われます。
2本以上を並列で書かせるときは記事ファイルのみ担当させ、導線はまとめて追加してください。

### `git add -A` は使わない

`.claude/settings.json` で禁止しています。追加するパスを毎回明示してください。

---

## 6. 記事の型（これが媒体の価値）

7記事すべてに共通する構造があります。**読者が「同じだろう」と思っている箇所が、実は違う**という事実を、
メーカー公式表示だけで示すことです。

| 記事 | 崩している思い込み |
| --- | --- |
| 猫砂 | 同じライオンペットでも、鉱物系は流せず、おから系は流せる |
| 洗濯用漂白剤 | 「酸素系だから大丈夫」ではない。ウール・絹への可否が3通りに分かれる |
| 洗濯用漂白剤 | 同じワイドハイターでも、EXパワーとPRO抗菌は成分が違う（容量・使用量は同じ） |
| キッチンスポンジ | 抗菌加工の表示は2製品だけ。しかも適用範囲の書き方が違う |
| 犬用ペットシーツ | 「レギュラー」は共通規格ではない。短辺が31〜34cmに分かれる |
| 猫用トイレ消臭シート | 「システムトイレ用」でも、自分の本体で使えるかは判断できない |

**レビューを一切使わずに書いています。** 公式表示の差だけで「判断が分かれる箇所」を示すのが型です。
新しい記事もこの型を維持してください。

---

## 7. 絶対にやらないこと

- Amazonのページ・検索結果・画像CDN・レビューをスクレイピングする
- 購入者レビューを転載・要約する（アソシエイト運営規約違反）
- 実使用していない製品の使用感・実測値・体験談を書く（景表法のステマ規制）
- ASINを推測でURLに組み立てる（別商品に誘導する）
- 効果を保証する書き方をする（「必ず落ちる」「99.9%除菌できます」）
  - 公式に「99.9%抗菌」と表示があれば「その表示がある」と書くのは可
- 比較対象の製品リンクに検索結果を使う（関連消耗品は一般名なので可）
- 秘密値・収益額をコミットする（リポジトリは公開）
- **公開ゲートを通すためにポリシーを緩める**

最後の項目が最も重要です。基準を満たせないなら、記事を出さないのが正しい判断です。

---

## 8. 既知のギャップ

- **PA-API 未接続。** ASINは手入力。適格販売3件の達成が前提
- **Pinterest 導線が未着手。** `erabi-pinterest-scout` と検証ツールは実装済みだが、
  比較表画像の作成が手作業のため1本も実行していない
- **収益データが未取得。** 計測の仕組みは動くが、実データを入れていない
- **アイリスオーヤマの公式サイトが HTTP 403。** WebFetchで本文を取得できず、
  同社製品は仕様を確認できない。curl で取得できた例もある（ドライシート）
- **Web検索に上限がある。** 1セッション200回。新規メーカーの探索を伴う調査は
  上限に注意（`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` で変更可）

---

## 9. 主要ファイル

| パス | 役割 |
| --- | --- |
| `content/pipeline/policy.json` | 公開ポリシー。**唯一の正** |
| `content/pipeline/queue.json` | 企画キューと状態機械 |
| `content/pipeline/specs/<slug>.json` | メーカー公式仕様（記事の根拠） |
| `content/pipeline/products/<slug>.json` | ASIN・直接リンク・画像 |
| `content/pipeline/asin-input/asins.csv` | ASIN記入シート（`.gitignore`） |
| `content/pipeline/revenue/` | 収益データ（`.gitignore`） |
| `.claude/agents/erabi-*.md` | 工程別エージェント6体 |
| `.claude/skills/erabi-pipeline/SKILL.md` | オーケストレータ |
| `tools/` | 検証・取得スクリプト（Node標準ライブラリのみ） |
| `docs/AUTOMATION.md` | パイプラインの詳細 |
| `docs/ASIN-INPUT.md` | ASIN記入の手順 |

---

## 10. Cloudflare Pages

| 項目 | 値 |
| --- | --- |
| Production branch | `main` |
| ビルドコマンド | `npm run build` |
| 出力ディレクトリ | `dist` |

`main` へ push すると本番ビルドが走ります。**CDNの反映に数分かかる**ため、
push直後に404や旧版が返ることがあります。数分待って再確認してください。

確認方法:

```bash
curl -sS -o /dev/null -w 'status=%{http_code}\n' -L 'https://erabi-note.jp/articles/<slug>/'
```
